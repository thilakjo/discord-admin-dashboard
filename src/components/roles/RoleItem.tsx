// RoleItem.tsx - Not explicitly used as a separate component in the current structure.
// The role item rendering logic is embedded directly within src/app/roles/page.tsx.
// You can keep this file empty or refactor the role item into it if desired.

import React from "react";

const RoleItem = () => {
  return null; // This component is not being rendered directly by other parts of the current code.
};

export default RoleItem;
